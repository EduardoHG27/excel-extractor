[phases.setup]
aptPkgs = ["python3.11", "python3.11-venv", "python3.11-dev"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate && gunicorn excel_extractor.wsgi"
