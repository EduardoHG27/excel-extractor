{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python -m pip install --upgrade pip && pip install -r requirements.txt && python manage.py collectstatic --noinput",
    "nixpacksConfig": {
      "providers": ["python"],
      "phases": {
        "setup": {
          "aptPkgs": ["python3.11", "python3.11-venv", "python3.11-dev"]
        }
      }
    }
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "python manage.py migrate && python manage.py fix_sequences && python manage.py shell -c \"from django.contrib.auth import get_user_model; User = get_user_model(); User.objects.filter(username='qa_adwin').exists() or User.objects.create_superuser('qa_adwin', 'eehernandez@buroidentidad.com', 'pa55w00rd_qa')\" && gunicorn excel_extractor.wsgi"
  }
}